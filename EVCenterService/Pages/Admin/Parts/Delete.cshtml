@page "{id:int}"
@model EVCenterService.Pages.Admin.Parts.DeleteModel
@{
    ViewData["Title"] = "Xóa Phụ Tùng";
    Layout = "_AppLayout";
}

<h2 class="mb-4 text-danger">Xác nhận Xóa Phụ Tùng</h2>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">@Model.ErrorMessage</div>
    <a asp-page="./Index" class="btn btn-primary">Quay lại Danh sách</a>
}
else
{
    <h3>Bạn có chắc chắn muốn xóa phụ tùng "@Model.Part.Name" không?</h3>
    <p>Hành động này sẽ xóa định nghĩa phụ tùng và các bản ghi kho liên quan (nếu không còn tồn kho và chưa từng được sử dụng). Hành động này không thể hoàn tác.</p>
    <hr />

    <div class="card data-card border-0">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Loại:</dt>
                <dd class="col-sm-9">@Model.Part.Type</dd>
                <dt class="col-sm-3">Hãng:</dt>
                <dd class="col-sm-9">@Model.Part.Brand</dd>
                <dt class="col-sm-3">Đơn giá:</dt>
                <dd class="col-sm-9">@Model.Part.UnitPrice?.ToString("N0") đ</dd>
            </dl>
        </div>
    </div>
    <br />
    <form method="post">
        <input type="hidden" asp-for="Part.PartId" />
        <button type="submit" class="btn btn-danger">Xác nhận Xóa</button>
        <a asp-page="./Index" class="btn btn-secondary">Hủy</a>
    </form>
}